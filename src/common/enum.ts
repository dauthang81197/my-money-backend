export enum Environment {
  local = 'local',
  production = 'production',
  dev = 'dev',
  test = 'test',
}

export enum SortCommonEnum {
  DATE_ASC = 'DATE_ASC',
  DATE_DESC = 'DATE_DESC',
  ASC = 'ASC',
  DESC = 'DESC',
}
